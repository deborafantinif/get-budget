version: "3"

services:
  node:
    image: node:16.14
    container_name: get_budget
    build: ./ 
    tty: true
    stdin_open: true
    command: bash
    restart: always
    working_dir: /server
    volumes:
      - ./:/server
    ports:
    - 3005:3005